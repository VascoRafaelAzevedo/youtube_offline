# Offline YouTube - Flutter App Configuration
# ============================================
# These are the default values used in the app.
# Users can change these in the app's Settings page.
# This file is just for documentation purposes.

# Default server URL (change IP to your server)
DEFAULT_SERVER_URL=http://192.168.1.100:8765

# Default API key (must match server's API_KEY)
DEFAULT_API_KEY=offline_yt_secret_2025

# Default video quality (max, 1080, 720, 360)
DEFAULT_QUALITY=1080
